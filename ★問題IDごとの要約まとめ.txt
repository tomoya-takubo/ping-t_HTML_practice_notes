30779：リスト関連である要素（ul, ol, li, dl）とそうでない要素（dt要素）について。
30822：canvas要素はJavascriptによって描画でき、幅と高さを指定しない場合、デフォルト値（300*150px）となる。
30821：div要素は、ブロックごとに、共通の属性（スタイル）を適用できる
30820：link要素は、そのHTML文書と別のファイルの関係を定義する。またtype属性で、RSSやAtomといったフィード（サイトの概要や更新情報）を指定する。
30819：a要素はhref属性の値としてid属性を指定できたり、入れ子にしてブロックレベル要素（divなど）を含められる。
30815：a要素ではhref属性の値に、tel:XXXのように電話番号を指定できる。
30814：aタグで囲まれた文字列はhref属性で指定した内容のハイパーリンクとなる。またtype属性でリンク先のタイプ（pdfとか）がわかる。
30806：（よくわからず。一旦飛ばす）
30805：script要素で読み込むJSスクリプトのtype属性ではデフォルト値（text/javascript）が用意されている。
30803：wbr要素を文字列内に挿入することで、文字列が描画領域に収まらない場合にその位置で改行するようにできる。
30801：ルビを振る。ruby > rb > rtの要素を押さえておく。
30800：ハイライト→mark要素、日時→time要素
30783：表の描画。table > tr > tdの要素を押さえておく。